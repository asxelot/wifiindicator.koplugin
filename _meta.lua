local _ = require("gettext")
return {
    fullname = _("Wi-Fi status icon"),
    description = _([[Replaces Wi-Fi connection popups with a small status icon in the top left corner of the screen, adds a tappable Wi-Fi status icon to the menu bar, and connects to Wi-Fi in the background without freezing the interface (Kobo, Kindle).]]),
}
